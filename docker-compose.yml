version: '3.8'

services:
  backend:
    build: .
    ports:
      - "3001:3001"
    environment:
      - PORT=3001
      - GLM_API_KEY=${GLM_API_KEY}
      - GLM_BASE_URL=https://open.bigmodel.cn/api/anthropic
    restart: unless-stopped
